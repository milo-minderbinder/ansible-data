---

#- name: install xcode command line tools
  #command: xcode-select --install

- include: homebrew-setup.yml
  tags:
    - homebrew-setup

- include: dotfiles.yml
  tags:
    - dotfiles

- include: gpg-config.yml
  tags:
    - gpg-config

